// Write a code to remove the first occurrence of a given “search string” from a string

let word = "Hello world" // Ho world (replace)
let searchWord = "ell"
let result = word.replace(searchWord, "")


console.log(result)